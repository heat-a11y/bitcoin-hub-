import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Bitcoin Knowledge Hub",
  description: "A curated collection of Bitcoin repositories — from newbies to advanced. Explore guides, tools, and resources across all skill levels.",
  openGraph: {
    title: "Bitcoin Knowledge Hub",
    description: "Curated Bitcoin repositories for every skill level.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-charcoal text-foreground">
        {children}
      </body>
    </html>
  );
}
